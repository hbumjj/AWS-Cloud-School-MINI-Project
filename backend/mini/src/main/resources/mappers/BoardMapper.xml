<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.amazoonS3.mini.mapper.BoardMapper">

    <select id="getAll" resultType="com.amazoonS3.mini.model.Board">
        SELECT b_idx, row_span, column_span, title, content, reg_date
        FROM board
        ORDER BY b_idx DESC
    </select>

    <insert id="insertBoard" useGeneratedKeys="true" keyProperty="bIdx">
        INSERT INTO board (row_span, column_span, title, content)
        VALUES (#{rowSpan}, #{columnSpan}, #{title}, #{content})
    </insert>

    <update id="updateBoard">
        UPDATE board
        SET row_span = #{rowSpan},
            column_span = #{columnSpan},
            title = #{title},
            content = #{content}
        WHERE b_idx = #{bIdx}
    </update>

    <select id="getBoardById" resultType="com.amazoonS3.mini.model.Board">
        SELECT b_idx, row_span, column_span, title, content, reg_date
        FROM board
        WHERE b_idx = #{bIdx}
    </select>

    <delete id="deleteBoard">
        DELETE FROM board WHERE b_idx = #{bIdx}
    </delete>

</mapper>